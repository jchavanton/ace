{# Rendered by ansible from ace_basic_auth_users. Each entry is a
   pre-hashed htpasswd line: `username:$2y$...`. Generate with
   `htpasswd -nbBC 10 <user> <password>` locally, then paste the whole
   line into the vault-encrypted list. #}
{% for entry in ace_basic_auth_users %}
{{ entry }}
{% endfor %}
